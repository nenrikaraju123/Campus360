package com.campus360.iam.domain;

/** Canonical role names, kept in sync with migration V1 (roles table seed). */
public enum RoleName {
    SUPER_ADMIN,
    INSTITUTION_ADMIN,
    HOD,
    FACULTY,
    STUDENT,
    PLACEMENT_OFFICER,
    RECRUITER,
    PARENT,
    ALUMNI
}
